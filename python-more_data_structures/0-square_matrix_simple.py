#!/usr/bin/python3


new_matrix = []
def square_matrix_simple(matrix=[]):
    for row in matrix
         new_row = list(map(lambda a: a**2, row))
         new_matrix.append(new_row)
    return new_matrix
